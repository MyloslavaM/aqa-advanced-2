//while
let index = 10;
const number = 9;

while (index > 0) {
	let result = index * number;
	console.log(`${index} * ${number} = ${result}`);
	index--;
}
console.log('***');
let indexAlternative = 1;
const numberAlternative = 7;
while (indexAlternative < 10) {
	let result = indexAlternative * numberAlternative;
	console.log(`${indexAlternative} * ${numberAlternative} = ${result}`);
	indexAlternative++;
}
